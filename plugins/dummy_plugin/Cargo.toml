[package]
name = "dummy_plugin"
version = "0.1.0"
edition = "2021"

[dependencies]
flux-plugin-sdk = { path = "../../sdk/flux-plugin-sdk" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[lib]
crate-type = ["cdylib"]
