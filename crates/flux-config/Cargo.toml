[package]
name = "flux-config"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1.0"
config = "0.14"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
toml = "0.8"
flux-storage = { path = "../flux-storage" }

[dev-dependencies]
tempfile = "3"
